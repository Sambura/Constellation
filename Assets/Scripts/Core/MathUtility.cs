using UnityEngine;

namespace Core
{
	static class MathUtility
	{
		public const float HalfPI = Mathf.PI / 2;
		public const float TwoPI = Mathf.PI * 2;
		public const float Angle0 = 0;
		public const float Angle90 = HalfPI;
		public const float Angle180 = Mathf.PI;
		public const float Angle270 = 1.5f * Mathf.PI;
		public const float Angle360 = TwoPI;
	}
}
